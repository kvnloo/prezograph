---
applyTo: "README.md,site/**,agents.md,skills/**,docs/**,package.json"
---

Keep interface claims aligned with the selected technical-preview release profile. JSON, browser,
local validate/stats CLI, Agent Skill source generation, and offline export are available. YAML,
API, MCP, persistent sharing, and registry publishing are planned.
