---
applyTo: "src/**,schemas/**,cli/**,scripts/**"
---

Preserve the safe-document profile. Treat deck fields as untrusted text. Add adversarial tests for
parser, URL, renderer, export, resource-limit, or interface changes. Keep the JSON Schema and runtime
validator aligned. Run `npm run check`.
