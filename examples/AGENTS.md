# Flagship content agent instructions

Own narrative, citations, entity facts, instances, edges, and beats. Do not change schema or
renderer semantics.

Use primary sources for dated claims, add ISO `sourceDate`, disclose conflicts, and label market
maps with an as-of date. Preserve globally unique instance IDs and validate with
`node cli/prezograph.mjs validate examples/graphs-are-awesome/deck.json`.
